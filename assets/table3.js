// Generating a second table with n columns and fill it with some data
const headers3 = generateHeaders(63, '');
const data3 = [
    ['[effet]              =>', ''],
    ['[targeted_file] =>','']

]; // Insérer vos données ici
createTable(2, 63, headers3, data3, 'tablesContainer');