// Generating a third table with n columns and fill it with some data
const headers3 = generateHeaders(86, '');
const data3 = [['ID', 'Info Y']]; // Insérer vos données ici
createTable(1, 86, headers3, data3, 'tablesContainer');
